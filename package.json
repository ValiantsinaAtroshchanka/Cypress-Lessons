{
  "scripts": {
    "cypress:open": "cypress open",
    "test": "echo\"Error: no test specified\" && exit 1"
  },
  "name": "lessons",
  "version": "1.0.0.",
  "devDependencies": {
    "cypress": "^10.1.0"
  }
}